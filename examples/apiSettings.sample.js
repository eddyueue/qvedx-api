// Configuration
var apiSettings = {
	//proxy: process.env.QVEDX_HTTP_PROXY || "http://localhost:3128",
    host: 'MY.QMC.HOST',
    ntlm: {user: 'DOMAIN\\USER:PASSWORD'}
};

module.exports = apiSettings;